# QualityScoreCard 六維度重新設計 - 開發總結

---
title: QualityScoreCard 六維度重新設計開發總結
date: 2026-01-23
author: Claude Code
status: completed
research_doc: thoughts/shared/research/2026-01-23-quality-score-card-redesign.md
guide_doc: thoughts/shared/guide/skill-quality-guide.md
---

## 1. 開發背景

根據 `thoughts/shared/guide/skill-quality-guide.md` 定義的新六維度品質評估標準，將前端 `QualityScoreCard` 元件從舊有六維度（architecture、maintainability、content、community、security、compliance）重構為新的六維度框架。

### 1.1 新六維度定義

| 維度                       | 英文代號        | 主要評估內容                       |
|----------------------------|-----------------|------------------------------------|
| **任務適配度與問題定義**   | problemFit      | 目標清晰度、工作流閉環、輸入精準度 |
| **正確性與可驗證性**       | correctness     | 方法論可重現、腳本吻合、範例可回歸 |
| **資料來源品質與資料治理** | dataGovernance  | 來源完整性、可追溯性、交叉驗證     |
| **穩健性與容錯**           | robustness      | 失敗模式處理、降級策略、錯誤回報   |
| **可重現性與可維護性**     | maintainability | 版本管理、模板穩定、避免文件漂移   |
| **輸出可用性與決策支援**   | usability       | 結論明確、依據完整、歷史對照       |

### 1.2 Badge 五等級

| Badge    | 分數區間 | 標準描述                 |
|----------|----------|--------------------------|
| **白金** | 90-100   | 頂級品質，可作為標竿參考 |
| **黃金** | 80-89    | 優質技能，經過充分驗證   |
| **白銀** | 60-79    | 合格技能，可正常使用     |
| **青銅** | 40-59    | 基本可用，需改進         |
| **入門** | 0-39     | 初期版本，謹慎使用       |

---

## 2. 完成項目

### 2.1 TypeScript 型別更新

**檔案**：`frontend/src/types/skill.ts`

新增/修改的型別定義：

```typescript
// 新六維度指標
export interface QualityMetrics {
  problemFit: number;       // 任務適配度與問題定義（0-100）
  correctness: number;      // 正確性與可驗證性（0-100）
  dataGovernance: number;   // 資料來源品質與資料治理（0-100）
  robustness: number;       // 穩健性與容錯（0-100）
  maintainability: number;  // 可重現性與可維護性（0-100）
  usability: number;        // 輸出可用性與決策支援（0-100）
}

// Badge 等級
export type QualityBadge = '白金' | '黃金' | '白銀' | '青銅' | '入門';

// 單一維度詳情
export interface MetricDetail {
  score: number;
  strengths: string[];      // 優點
  improvements?: string[];  // 待改進項目（可選）
}

// 升級建議
export interface UpgradeNote {
  targetBadge: QualityBadge;
  requirements: {
    metric: keyof QualityMetrics;
    currentScore: number;
    targetScore: number;
    suggestion: string;
  }[];
}

// 舊版指標（向後相容）
export interface LegacyQualityMetrics {
  architecture?: number;
  maintainability?: number;
  content?: number;
  community?: number;
  security?: number;
  compliance?: number;
}

// 質量評分介面
export interface QualityScore {
  overall: number;
  badge: QualityBadge | string;
  metrics: QualityMetrics | LegacyQualityMetrics;
  details?: string;
  metricDetails?: Record<keyof QualityMetrics, MetricDetail>;
  upgradeNotes?: UpgradeNote;
  evaluatedAt?: string;
}
```

### 2.2 新建元件清單

| 元件                  | 檔案路徑                                         | 功能說明                   |
|-----------------------|--------------------------------------------------|----------------------------|
| QualityBadge          | `frontend/src/components/skills/QualityBadge.tsx`       | 五等級徽章元件             |
| QualityMetricCard     | `frontend/src/components/skills/QualityMetricCard.tsx`  | 單一維度卡片（可展開詳情） |
| QualityMetricGrid     | `frontend/src/components/skills/QualityMetricGrid.tsx`  | 六維度網格佈局             |
| QualityRadarChart     | `frontend/src/components/skills/QualityRadarChart.tsx`  | 純 SVG 雷達圖              |
| QualityUpgradePath    | `frontend/src/components/skills/QualityUpgradePath.tsx` | 升級路徑指引               |
| QualityScoreCard      | `frontend/src/components/skills/QualityScoreCard.tsx`   | 主容器（整合所有子元件）   |

---

## 3. 元件詳細說明

### 3.1 QualityBadge 徽章元件

**功能**：顯示品質等級徽章，包含圖示、分數與英文標籤

**特點**：
- 五等級配色方案（白金/黃金/白銀/青銅/入門）
- 支援三種尺寸（sm/md/lg）
- 向後相容：非標準 badge 值會根據分數自動推斷等級
- 漸層背景與陰影效果

**配色方案**：

| Badge | 圖示 | 漸層背景                        | 邊框               | 英文標籤 |
|-------|------|--------------------------------|--------------------|----------|
| 白金  | 💎   | from-slate-100 to-slate-200   | border-slate-400   | PLATINUM |
| 黃金  | 🏆   | from-amber-100 to-yellow-100  | border-amber-400   | GOLD     |
| 白銀  | 🥈   | from-gray-100 to-slate-100    | border-gray-400    | SILVER   |
| 青銅  | 🥉   | from-orange-100 to-amber-100  | border-orange-400  | BRONZE   |
| 入門  | 🌱   | from-gray-50 to-gray-100      | border-gray-300    | STARTER  |

### 3.2 QualityMetricCard 維度卡片元件

**功能**：顯示單一維度的分數、等級與詳情

**特點**：
- 每個維度獨立配色（藍、綠、紫、橘、灰、青）
- 進度條視覺化
- 可展開的詳情區塊（優點/待改進清單）
- 支援精簡模式（compact）

**維度配置**：

| 維度            | 圖示 | 中文名     | 簡稱 | 配色   |
|-----------------|------|------------|------|--------|
| problemFit      | 🎯   | 任務適配度 | 適配 | 藍色   |
| correctness     | ✅   | 正確性     | 正確 | 綠色   |
| dataGovernance  | 📊   | 資料治理   | 資料 | 紫色   |
| robustness      | 🛡️   | 穩健性     | 穩健 | 橘色   |
| maintainability | 🔧   | 可維護性   | 維護 | 灰色   |
| usability       | 📋   | 輸出可用性 | 可用 | 青色   |

**分數等級對應**：

| 分數區間 | 等級文字 | 顏色         |
|----------|----------|--------------|
| 90-100   | 優秀     | 翠綠色       |
| 80-89    | 良好     | 綠色         |
| 60-79    | 中等     | 黃色         |
| 40-59    | 待改進   | 橘色         |
| 0-39     | 需加強   | 紅色         |

### 3.3 QualityMetricGrid 網格元件

**功能**：管理六維度卡片的網格佈局

**特點**：
- 響應式網格：手機 1 欄、平板 2 欄、桌面 3 欄
- 自動偵測新舊版指標，分別渲染
- 向後相容：舊版指標使用原有簡化樣式

### 3.4 QualityRadarChart 雷達圖元件

**功能**：純 SVG 實作的六邊形雷達圖

**特點**：
- 無需外部圖表套件
- 5 層同心六邊形背景網格（20/40/60/80/100）
- 從中心到各頂點的輔助線
- 資料區域半透明填充 + 邊框線
- 各頂點顯示維度標籤與分數
- 僅新版六維度才渲染（舊版返回 null）

**維度排列（順時針）**：
1. problemFit（12 點鐘）
2. correctness（2 點鐘）
3. dataGovernance（4 點鐘）
4. robustness（6 點鐘）
5. maintainability（8 點鐘）
6. usability（10 點鐘）

### 3.5 QualityUpgradePath 升級路徑元件

**功能**：顯示從當前等級升級到目標等級的路徑與建議

**特點**：
- 當前等級 → 目標等級的視覺化指示器
- 各維度升級需求卡片（含進度條）
- 每項需求顯示：
  - 維度名稱與圖示
  - 當前分數 → 目標分數（差距）
  - 進度百分比
  - 改進建議
- 總結提示：完成 N 項改進即可升級

### 3.6 QualityScoreCard 主容器元件

**功能**：整合所有子元件的品質評估卡片

**佈局結構**：

```
┌─────────────────────────────────────────────┐
│  Header: ✨ 品質評估         [QualityBadge] │
├─────────────────────────────────────────────┤
│  ┌─────────────────┐ ┌────────────────────┐ │
│  │                 │ │ 70                 │ │
│  │  RadarChart     │ │ 總分（六維度平均） │ │
│  │                 │ │ [迷你進度條群組]   │ │
│  │                 │ │ 評估日期：...      │ │
│  └─────────────────┘ └────────────────────┘ │
├─────────────────────────────────────────────┤
│  維度評分                                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ 適配 75 │ │ 正確 80 │ │ 資料 65 │       │
│  └─────────┘ └─────────┘ └─────────┘       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ 穩健 65 │ │ 維護 70 │ │ 可用 75 │       │
│  └─────────┘ └─────────┘ └─────────┘       │
├─────────────────────────────────────────────┤
│  [查看詳情 ▼]  [📈 查看升級路徑 ▼]          │
├─────────────────────────────────────────────┤
│  (展開後) Markdown 詳情                      │
│  (展開後) QualityUpgradePath                 │
└─────────────────────────────────────────────┘
```

**特點**：
- 新版六維度：顯示雷達圖 + 總分摘要
- 舊版指標：僅顯示總分
- 可摺疊的 Markdown 詳情區塊
- 可摺疊的升級路徑區塊

---

## 4. 向後相容設計

### 4.1 指標偵測邏輯

```typescript
function isNewMetrics(metrics): metrics is QualityMetrics {
  return 'problemFit' in metrics ||
         'correctness' in metrics ||
         'dataGovernance' in metrics;
}
```

### 4.2 Badge 推斷邏輯

```typescript
function inferBadgeFromScore(score: number): QualityBadge {
  if (score >= 90) return '白金';
  if (score >= 80) return '黃金';
  if (score >= 60) return '白銀';
  if (score >= 40) return '青銅';
  return '入門';
}
```

### 4.3 相容行為

| 情境                     | 處理方式                           |
|--------------------------|------------------------------------|
| 舊版 metrics             | 使用舊版網格樣式，不顯示雷達圖     |
| badge 值非標準字串       | 根據 overall 分數自動推斷等級      |
| 無 metricDetails         | 維度卡片不顯示展開功能             |
| 無 upgradeNotes          | 不顯示「查看升級路徑」按鈕         |
| 無 evaluatedAt           | 不顯示評估日期                     |

---

## 5. 新 skill.yaml qualityScore 結構

```yaml
qualityScore:
  overall: 70
  badge: 白銀
  evaluatedAt: "2026-01-23"

  metrics:
    problemFit: 75        # 任務適配度與問題定義
    correctness: 80       # 正確性與可驗證性
    dataGovernance: 65    # 資料來源品質與資料治理
    robustness: 65        # 穩健性與容錯
    maintainability: 70   # 可重現性與可維護性
    usability: 75         # 輸出可用性與決策支援

  metricDetails:
    problemFit:
      score: 75
      strengths:
        - SKILL.md 有清晰的一句話目標
        - workflows/analyze.md 覆蓋主路徑
      improvements:
        - 缺少不適用情境說明

    correctness:
      score: 80
      strengths:
        - methodology.md 有完整公式推導
        - examples/ 有 1 個 golden case
      improvements:
        - 可增加更多邊界案例

    # ... 其他維度 ...

  details: |
    **任務適配度（75/100）**
    - SKILL.md 有清晰的一句話目標
    ...

  upgradeNotes:
    targetBadge: 黃金
    requirements:
      - metric: dataGovernance
        currentScore: 65
        targetScore: 70
        suggestion: 增加 fallback 來源，fetch 輸出增加 timestamp
      - metric: robustness
        currentScore: 65
        targetScore: 70
        suggestion: 明確缺值處理策略，增加降級輸出機制
```

---

## 6. 檔案變更總覽

| 操作 | 檔案路徑                                                |
|------|---------------------------------------------------------|
| 修改 | `frontend/src/types/skill.ts`                           |
| 修改 | `frontend/src/components/skills/QualityScoreCard.tsx`   |
| 新增 | `frontend/src/components/skills/QualityBadge.tsx`       |
| 新增 | `frontend/src/components/skills/QualityRadarChart.tsx`  |
| 新增 | `frontend/src/components/skills/QualityMetricGrid.tsx`  |
| 新增 | `frontend/src/components/skills/QualityMetricCard.tsx`  |
| 新增 | `frontend/src/components/skills/QualityUpgradePath.tsx` |

---

## 7. 後續建議

### 7.1 短期任務

1. **skill.yaml 遷移**
   - 將所有 skills/ 目錄下的 skill.yaml 遷移至新結構
   - 可建立遷移腳本自動轉換舊維度至新維度

2. **測試驗證**
   - 測試新舊版 skill.yaml 的相容性
   - 驗證各種分數組合的視覺呈現
   - 響應式佈局測試

### 7.2 中期優化

1. **動畫效果**
   - 雷達圖繪製動畫
   - 分數計數動畫
   - 卡片展開/收合動畫

2. **SkillCard 整合**
   - 在列表頁的技能卡片顯示小型 Badge

### 7.3 長期功能

1. **比較模式**
   - 多個 Skill 的品質比較視圖

2. **歷史趨勢**
   - 品質分數隨版本變化的趨勢圖

3. **評估自動化**
   - 建立腳本自動評估各維度分數

---

## 8. 相關文件

| 文件類型 | 路徑                                                       |
|----------|------------------------------------------------------------|
| 研究報告 | `thoughts/shared/research/2026-01-23-quality-score-card-redesign.md` |
| 品質指南 | `thoughts/shared/guide/skill-quality-guide.md`             |

---

*開發完成日期：2026-01-23*
